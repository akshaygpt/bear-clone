# Bear Clone — V1 Plan

## 1. Add remaining backend endpoints
- `POST /notes` — create a new note
- `PATCH /notes/{id}` — update an existing note
- `DELETE /notes/{id}` — delete a note

## 2. Build the frontend
- Replace the default Next.js placeholder in `page.tsx`
- Build a Bear-like UI: note list on the left, editor on the right

## 3. Connect frontend to backend
- Wire the frontend to the API (`http://localhost:8000`)
- CRUD operations should reflect in real time on the UI
